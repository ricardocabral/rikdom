# Roadmap Issue Specs

This directory contains issue-ready Markdown files.

Create GitHub issues from these files after the repository is pushed:

```bash
python3 scripts/create_github_issues.py --repo ricardocabral/rikfom
```

The script will create issues in filename order.
