# Canonical Schema v1 + Compatibility Policy

## Parent PRD

Initial rikdom PRD (Codex thread, 2026-04-20)

## What to build

Finalize canonical JSON contracts (`portfolio`, `snapshot`, plugin statement), including `schema_uri`, semantic versioning, and compatibility policy (reader/writer behavior).

## Acceptance criteria

- [ ] Core schema files define required contract identity fields.
- [ ] Compatibility rules are documented in docs.
- [ ] Validation command reports version-compatibility mismatches.

## Blocked by

None - can start immediately.

## User stories addressed

- Long-lived portable schema.
- Safe schema evolution over years.
